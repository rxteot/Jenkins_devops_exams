apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "movie-platform.fullname" . }}-movie-service
  labels:
    app: movie-service
spec:
  replicas: {{ .Values.movie_service.replicas }}
  selector:
    matchLabels:
      app: movie-service
  template:
    metadata:
      labels:
        app: movie-service
    spec:
      containers:
        - name: movie-service
          image: "{{ .Values.movie_service.image.repository }}:{{ .Values.movie_service.image.tag }}"
          command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URI
              value: "{{ .Values.movie_service.env.DATABASE_URI }}"
            - name: CAST_SERVICE_HOST_URL
              value: "{{ .Values.movie_service.env.CAST_SERVICE_HOST_URL }}"
