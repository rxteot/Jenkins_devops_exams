apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "movie-platform.fullname" . }}-test-connection"
  labels:
    {{- include "movie-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "movie-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
